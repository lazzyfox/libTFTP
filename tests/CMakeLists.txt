cmake_minimum_required(VERSION 3.0.0)
project(tftp_test VERSION 0.0.1)
set(CMAKE_CXX_STANDARD 20)
set (CMAKE_CXX_COMPILER_VERSION 11)
set(CMAKE_CXX_COMPILER "/usr/bin/g++-11")
set(CMAKE_C_COMPILER ${C_COMPILER})
set(CMAKE_CXX_FLAGS "-std=c++20 -Wall -Wextra -fPIC")
# Find additional packages
find_package(GTest CONFIG REQUIRED)
#find_package(CURL::libcurl CONFIG REQURED)
# Creating tests
include(CTest)
enable_testing()
find_package(GTest CONFIG REQUIRED)
#add_subdirectory(tests)
#target_include_directories(libtftp_test PRIVATE tests/include)
add_test(NAME Test COMMAND libtftp_test)
set(CPACK_PROJECT_NAME ${PROJECT_NAME})
set(CPACK_PROJECT_VERSION ${PROJECT_VERSION})
include(CPack)
# Finial test
add_executable(tftp_test test.cpp)
target_link_libraries(tftp_test PRIVATE GTest::gtest)
